services:
  react:
    image: react
    container_name: react
    restart: unless-stopped
    ports:
      - "80"
    networks:
      - npm_shared_net

networks:
  npm_shared_net:
    name: npm-network
    external: true
